{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Contribution Lands Theme",
  "type": "object",
  "required": ["name", "category", "background", "ground_colors"],
  "properties": {
    "name": { "type": "string", "description": "Display name of the theme" },
    "category": { "type": "string", "enum": ["forest", "city"], "description": "Theme category" },
    "author": { "type": "string" },
    "background": { "type": "string", "description": "Canvas background color (hex)" },
    "ground_colors": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Isometric ground tile colors (hex)"
    },
    "ground_stroke": { "type": "string", "description": "Ground tile outline color (hex)" },
    "ui_accent": {
      "type": "string",
      "description": "Optional popup accent color used for extension chrome and highlights"
    },
    "preview_sprites": {
      "type": "object",
      "description": "Optional popup preview sprite filenames keyed by contribution level",
      "properties": {
        "0": { "type": "string" },
        "1": { "type": "string" },
        "2": { "type": "string" },
        "3": { "type": "string" },
        "4": { "type": "string" }
      },
      "additionalProperties": false
    },
    "entities": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["type", "count", "speed"],
        "properties": {
          "type": { "type": "string" },
          "count": { "type": "integer" },
          "speed": { "type": "number" },
          "flyer": { "type": "boolean" }
        }
      }
    },
    "particles": {
      "type": ["object", "null"],
      "properties": {
        "type": { "type": "string", "enum": ["snow", "leaves", "petals"] },
        "count": { "type": "integer" },
        "colors": { "type": "array", "items": { "type": "string" } }
      }
    }
  }
}
